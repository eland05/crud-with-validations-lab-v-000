<h1>Songs</h1>

<table>
  <head>
    <tr>
      <th>Title</th>
      <th>Year</th>
      <th>Genre</th>
      <th>Artist Name</th>
      <th>Links</th>
    </tr>
  </head>

  <tbody>
    <% @songs.each do |song| %>
      <tr>
        <td><%= song.title %></td>
        <td><%= song.title %></td>
        <td><%= song.title %></td>
        <td><%= song.title %></td>
        <td></td>
      </tr>
    <% end %>
  </tbody>
</table>

    
